/** Staff / campus photo for Apply band + footer fog background. */
export const FOOTER_SECTION_BG = "/images/footer-section-bg.png?v=nurses2";

export function footerSectionPhoto(): string {
  return `url('${FOOTER_SECTION_BG}')`;
}
